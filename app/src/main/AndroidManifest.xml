<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — SmartTrip

    MODIFICATIONS par rapport à l'original :
    - Ajout de ActiveTripActivity (voyage en cours)
    - Ajout de SettingsActivity (paramètres)
    - Ajout de la permission BATTERY_STATS (lecture niveau batterie)

    NOTE : Les permissions GPS (ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION)
    seront nécessaires quand tu intègreras le vrai GPS.
    Pour l'instant la collecte est simulée, donc pas besoin.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smarttrip">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SmartTrip"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartTrip"
        android:usesCleartextTraffic="true">

        <!-- Splash Screen (launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Écran principal -->
        <activity android:name=".MainActivity" />

        <!-- Historique des voyages -->
        <activity android:name=".TripsActivity" />

        <!-- Détail d'un voyage -->
        <activity android:name=".TripDetailsActivity" />

        <!-- Voyage en cours (NOUVEAU) -->
        <activity android:name=".ActiveTripActivity" />

        <!-- Paramètres (NOUVEAU) -->
        <activity android:name=".SettingsActivity" />

        <!-- Après les autres <activity>, avant </application> -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.smarttrip.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
